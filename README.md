# sol

Next steps:

1. Run `npm install` at the repository root to install dependencies.
2. Run Storybook for the design system after install:

```bash
# from repo root
npm run storybook
```

Apps in `apps/` and libraries in `libs/`.
